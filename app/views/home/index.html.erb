<% content_for :navbar_links do %>
  <% if current_user && current_user.business  %>
    <li><%= link_to "My Business",  business_admin_path%></li>
  <% end %>

  <% if current_user %>
    <li><%= link_to "Profile", profile_path %></li>
  <% else %>
    <li><%= link_to "Sign Up", logout_path %></li>
  <% end %>
<% end %>
<div class="gh-hero-content-container">
  <div class="gh-hero-top-padding"></div>
    <div class="col-md-5">
      <div class='gh-tagline col-centered'>GIT A DAMN JOB</div>
      <%= link_to "Browse Jobs", jobs_path, class: "gh-browse-jobs col-md-6 col-centered btn btn-default" %>
    </div>
    <div class="gh-hero-bottom-padding"></div>
</div>
<div class='gh-hero-content-footer'>
  <div class="gh-hero-top-padding"></div>
  <div class=" col-md-5">
    <span class="glyphicon glyphicon-chevron-down"></span>
  </div>
  <div class="gh-hero-bottom-padding"></div>
</div>
